{
  "name": "cholog-sdk",
  "version": "0.0.1",
  "main": "dist/cholog.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:npm": "esbuild src/index.ts --bundle --format=esm --outfile=dist/cholog.mjs",
    "build:cdn": "esbuild src/index.ts --bundle --format=iife --global-name=Cholog --outfile=dist/cholog.min.js --minify",
    "build": "npm run build:npm && npm run build:cdn"
  },
  "keywords": [
    "logger",
    "logging",
    "cholog",
    "초록",
    "로깅"
  ],
  "contributors": [
    "simgyubin <skb0606@gmail.com> (https://github.com/SIM-GYUBIN)"
  ],
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
